# Upstash Vector Database (Default - used when BYOK is not enabled)
UPSTASH_VECTOR_REST_URL=https://your-vector-index.upstash.io
UPSTASH_VECTOR_REST_TOKEN=your-vector-token

# Upstash Redis (Required - for job status tracking between scraper and main app)
# Note: Redis credentials must always be set as they're used for job status communication
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# Scraper Settings
SCRAPER_CONCURRENCY=3
SCRAPER_TIMEOUT=30000
SCRAPER_ENGINE=cheerio  # cheerio or puppeteer

# Callback Settings (Optional - for notifying main app when done)
CALLBACK_URL=https://your-app.vercel.app/api/setup/scraper-callback
CALLBACK_SECRET=your-secret-key

# =========================================
# BYOK (Bring Your Own Keys) - Dynamic Credentials
# =========================================
# These are passed dynamically via GitHub Actions when BYOK is enabled for an app.
# You don't need to set these here - they're provided by the main app's dispatch payload.
# Supported via CLI args or environment variables:
#
# BYOK_VECTOR_DB_PROVIDER=upstash  # upstash, pinecone, weaviate
# BYOK_VECTOR_DB_URL=https://customer-index.upstash.io
# BYOK_VECTOR_DB_TOKEN=customer-token
# BYOK_VECTOR_DB_INDEX=my-index
# BYOK_VECTOR_DB_NAMESPACE=my-namespace
#
# CLI equivalent:
# --vector-db-provider=upstash
# --vector-db-url=https://...
# --vector-db-token=...
# --vector-db-index=my-index
# --vector-db-namespace=my-namespace
